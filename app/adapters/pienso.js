import DS from 'ember-data';

export default DS.RESTAdapter.extend({

 host: 'http://localhost:3000'
 });


//Could not find module `pienso/adapters/application` imported from `pienso/adapters/pienso`
